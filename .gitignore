# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
packaging/stage/
wheels/
*.egg-info

# Virtual environments
.venv

# Sync / app logs (local)
logs/

# Local secrets (encrypted credentials DB + Fernet key — never commit)
sophos_secrets.db
sophos_secrets.db-*
sophos_credential_key
sophos_session_secret

# Env files and local app database (never commit)
credentials.env
.env
sophos_central.db
sophos_central.db-wal
sophos_central.db-shm
*.db